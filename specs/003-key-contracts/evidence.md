# 步骤 1 验证记录

日期：2026-09-10。验证范围为可实施合同与示例，不是业务服务运行。

## 自动检查 AC-01 / AC-02 / AC-05

命令：`python specs/003-key-contracts/verify_contracts.py`（仓库根目录）。

检查 12 个独立固定帧的头长度/方向、BER 容器、登录必填顺序及正整数编码、认证拒绝诊断、GET 对象/数值/缩放、每个帧的所有截断位置与粘包、非法版本和超长头；检查 6 个 HTTP/MQ JSON 示例语法及跨层关联、结果密码边界；检查本迭代、根规范和实施步骤的本地文件链接。

首次执行：样本结构/关联检查通过，链接检查因证据文件尚未创建失败；创建本文件后重新运行，以终端最终 PASS 为完成依据。脚本是文档检查器，无 Common/Java 实现；不声称做了业务编解码 round-trip 或 IEC 标准一致性测试。

最终执行：退出码0，12帧、全部切分位置/粘包、非法头、BER、GET/缩放、6个JSON示例、关联和密码边界全部 PASS；本地链接全部有效。`git -c safe.directory=D:/AAASTUDY/work/xxq diff --check` 退出码0，仅提示既有文件下次 Git 操作会转换 LF/CRLF，无空白错误。safe.directory 只对该命令生效，未改全局配置。

## 手工合同推演 AC-03 / AC-04

| 输入/事件 | 按合同得到的输出 | 结论 |
| --- | --- | --- |
| 两表三项，表A第二项访问拒绝，其他成功 | 两个 meterTask 各三行；每表逐项推进，跨表并行；最终 total=6/completed=6/succeeded=5/failed=1，PARTIAL_FAILED、done=true | 规则闭合 |
| 页面离开后恢复 | abort 和清 timer；服务器继续，同 taskId 返回完整六行；无游标缺口；终态停止轮询 | 规则闭合 |
| 终态后24小时查询；墓碑再过24小时 | 先410，墓碑清除后404；无自动再次提交 | 规则闭合 |
| timeoutMs=5000/retryCount=1，第一次在t=0发出 | t=5s 旧尝试终止并关闭旧连接，t=5.5s 新尝试，新 deadline=10.5s；t=5.8s 旧响应不能覆写新尝试；第二次超时后行 FAILED | 有限等待、旧包隔离 |
| broker 重投同 attemptId | 执行中不再次 GET；已完成重发缓存；UCI 重复结果不增加 completed | 幂等规则明确 |
| 不同批次同时选表A，另有表B | A后批次排队，B可独立启动；A排队超过60秒逐项 QUEUE_TIMEOUT | 串并行边界明确 |
| AARE 密码拒绝 | 不发 GET，不重试该项；后续项仍需独立形成结果，不返回读数 | 认证失败闭合 |
| 心跳距上次恰540秒/超过540秒；旧连接清理回调 | 恰好不离线，超过时下一次1秒检查断开；compare connectionId 不误删新连接 | 边界明确 |
| UCI或Channels重启 | UCI内存丢失返回404；Channels关闭旧TCP，重投可能重复只读GET，UCI终态仍至多结算一次 | 限制明确，无虚假恢复承诺 |

## 来源与状态检查

保留 00–11 HTML；原文冲突在 reference 中记录，新增设计有明确标识。四类基线已同步，根 T-01 仅为合同任务完成；T-02 因 Q-06 其余部分/Q-07 保持未完成，T-05–T-16 仍未勾选。无新增框架、依赖、数据库表或业务实现；未运行前端构建及服务联调，因为此次只变更合同和文档样本。

后续必须补充 Common 单元测试、独立协议译码证据、ActiveMQ 重投集成测试和四服务端到端证据；本记录不能替代这些验收。
