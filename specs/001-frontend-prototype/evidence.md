# 原型验证证据

日期：2026-09-08；环境：Windows、Node 24.10.0、npm 11.6.1。命令在 frontend/ 执行。

## 工程检查

| 检查 | 实际结果 |
| --- | --- |
| npm run lint -- --fix | 最终 0 错误、0 警告；修复 index.vue 显式组件名和浏览器全局变量声明 |
| npm run build | vue-tsc 通过，Vite 构建成功；1616 modules，输出 dist/；三个页面动态导入 |
| npm test | 5 项通过（原有 HTTP 1 项 + 原型 4 项），0 失败 |
| git diff --check | 通过，仅本机 CRLF 提示 |

原型测试文件：frontend/tests/prototype.test.ts。覆盖禁用状态 0、组合筛选、重复表号与编辑排除当前记录、空名称、相同 OBIS 不同属性、结果数值 0、失败无读数、日志闭区间时间筛选和 CSV 引号/换行/公式防护。

## 浏览器操作记录

本地 Vite http://127.0.0.1:5173，Codex 内置浏览器；桌面默认视口、390px 窄屏检查。

| 场景 | 操作与可观察结果 |
| --- | --- |
| AC-01 导航 | 点击三个侧边菜单均到达对应页面；访问 /#/unknown 回到 /#/meters；刷新恢复 12 条初始档案；菜单高亮跟随路由 |
| AC-01 目录 | 三页均使用 views/<page>/index.vue，各有 components/ 与 types/index.ts；HomeView.vue 已被替换；FE-01a 写入共享前端规范 |
| AC-02 筛选 | 搜索“不存在”显示空状态；重置恢复；翻页显示下一组电表 |
| AC-02 表单 | 空表单提示必填；表号 100000000001 提示重复；保存 TEST-001 / 浏览器验证表后总数变为 13；编辑为“验证表已编辑”后列表更新 |
| AC-02 删除 | 按名称筛选验证表，确认删除后出现空状态；内存数据移除 |
| AC-02 样例导入 | 固定 3 行显示 1 条有效、重复和缺表号各 1 条；点击确认后只新增 1 条，按钮禁用避免重复；跨页可见新增演示档案 |
| AC-02 导出 | 筛选验证表，导出电表档案-演示.csv；检查下载文件含表头与唯一 TEST-001 行，无其他记录 |
| AC-03 正常演示 | 初始空选择禁用开始；选两表三项后共 6 项；运行时每表首项读取、后两项等待，按钮及选择被禁用；最终 6 成功，电流显示 0 A |
| AC-03 认证失败 | 末台认证失败场景：首台 3 成功、末台 3 失败；失败值均为 —，说明 LLS 失败未执行 GET |
| AC-03 超时与对象 ID | 选择电压属性 2 和属性 3 等共 4 项，超时场景返回 4 失败；两个电压属性保持独立 ID |
| AC-03 离页 | 演示开始后切日志再返回，结果区恢复初始空状态、选择清空，无前任务结果回填；实现通过 onBeforeUnmount 清理 interval |
| AC-04 日志 | 搜索“认证失败”得到 1 条 ERROR/Channels；详情显示 LOG-004、北京时间、追踪 ID 和完整消息；组合级别/服务/时间条件由纯函数测试覆盖 |
| AC-04 导出 | 系统日志-演示.csv 实际下载，检查内容仅有该条匹配日志及表头，含完整消息 |
| AC-05 窄屏 | 390px 下日志页/档案页 document scrollWidth 等于 clientWidth=375（预留滚动条），无整体横向溢出；导航变为顶部菜单可点击；新增弹窗 left=16、right≈374，保持在屏内 |
| AC-05 运行日志 | 浏览器读取 error/warn 为空 |

下载事件接口两次未回传 download，后续在本机 Downloads 中确认文件实际已生成，并直接核对 CSV 内容，因此下载功能验证通过。

## 范围限制

所有数据和任务都是演示夹具。未验证正式 HTTP DTO、真实文件导入、生产超时重试、LLS/GET、四服务联调、日志文件索引或生产部署；Q-02/Q-04/Q-06/Q-07 仍待正式合同确认。不宣称完整多浏览器兼容、生产可用或真实业务验收通过。
